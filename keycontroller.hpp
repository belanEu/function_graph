#include "defs.hpp"
 
 #ifndef KEY_CONTROLLER_H
 #define KEY_CONTROLLER_H

class KeyController {
    public:

    const static char goTop = 'w',
                    goBottom = 's',
                    goLeft = 'a',
                    goRight = 'd',
                    exit = 'q';

};

 #endif